# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.*

# tool artifacts
.lighthouseci/
*.log
npm-debug.log*

# local tooling (cloned skill marketplaces, local settings — not project code)
.claude/skills/
.claude/settings.local.json
claude-skills/
.superpowers/
.agents/
skills-lock.json

# reference + debug images dropped in the repo root. Root-anchored on purpose:
# real source images live in src/assets and must stay tracked. Client reference
# material (mood boards, colour studies) never enters the repo.
/*.png
/*.jpg
/*.jpeg

# OS
Thumbs.db
.DS_Store
