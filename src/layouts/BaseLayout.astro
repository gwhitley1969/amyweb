---
import '@fontsource-variable/playfair-display';
import '@fontsource-variable/dm-sans';
import playfairWoff2 from '@fontsource-variable/playfair-display/files/playfair-display-latin-wght-normal.woff2?url';
import '../styles/global.css';
import SeoHead from '../components/SeoHead.astro';
import { localBusiness } from '../lib/schema';

interface Props {
  title: string;
  description: string;
  /** Page surface: light "studio" (default) or dark "sign" (BUILD_SPEC §5 plan). */
  surface?: 'light' | 'noir';
  ogImage?: string | undefined;
}

const { title, description, surface = 'light', ogImage } = Astro.props;
const jsonLd = JSON.stringify(localBusiness());
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <SeoHead title={title} description={description} ogImage={ogImage} />
    {/* Favicons derived from the logo (scripts/derive-logo.mjs on phase-c, 2026-09-15):
        the ICO carries 16/32/48; iOS takes the 180px tile. */}
    <link rel="icon" href="/favicon.ico" sizes="16x16 32x32 48x48" />
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
    <meta name="generator" content={Astro.generator} />
    {/* Display face only (§13: preload the display weight); DM Sans relies on swap + metric fallbacks. */}
    <link rel="preload" href={playfairWoff2} as="font" type="font/woff2" crossorigin />
    <script type="application/ld+json" set:html={jsonLd} />
  </head>
  <body class="min-h-dvh" data-surface={surface === 'noir' ? 'noir' : undefined}>
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:bg-white focus:p-3 focus:text-black focus:underline"
    >
      Skip to content
    </a>
    <header>
      <slot name="header" />
    </header>
    <main id="main">
      <slot />
    </main>
    <footer>
      <slot name="footer" />
    </footer>
  </body>
</html>
