---
import ConceptHome from '../components/ConceptHome.astro';

/**
 * / — THE HOME PAGE (C8 flip, 2026-08-04; DECISIONS same date). Renders
 * ConceptHome, the client-approved Neon Editorial build — the component
 * carries its own BaseLayout, Header/Footer, and production SEO strings.
 * /styleguide/concept keeps rendering the same component as the
 * preview-only mirror (production builds emit no styleguide).
 *
 * The construction placeholder that lived here 2026-07-07 → 2026-08-04
 * is retired; its brand assets (logo render, caricature) stay in
 * src/assets/brand/ for future reuse, and its legacy keyframes
 * (ng-ignite/ng-hum) were deleted from global.css with this flip.
 */
---

<ConceptHome />
